services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pclzw-app
    restart: always
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=production
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
